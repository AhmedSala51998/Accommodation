# 📘 دليل النظام الشامل - نظام إدارة سكن العاملات v2.0

## 🎯 المحتويات:

1. [المميزات الجديدة](#المميزات-الجديدة)
2. [البدء السريع](#البدء-السريع)
3. [صفحات النظام](#صفحات-النظام)
4. [الصلاحيات](#الصلاحيات)
5. [سجل الأنشطة](#سجل-الأنشطة)
6. [أمثلة عملية](#أمثلة-عملية)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🌟 المميزات الجديدة

### 1. نظام تسجيل الدخول الآمن
- ✅ تشفير كلمات المرور باستخدام bcrypt
- ✅ جلسات آمنة
- ✅ منع الوصول بدون تسجيل دخول
- ✅ تسجيل حاولات الدخول

### 2. إدارة المستخدمين والأدوار
- ✅ إضافة وتعديل وحذف المستخدمين
- ✅ 4 أدوار افتراضية
- ✅ إدارة الصلاحيات حسب الدور
- ✅ تفعيل/تعطيل المستخدمين

### 3. سجل الأنشطة الشامل
- ✅ تسجيل جميع العمليات
- ✅ تتبع التغييرات (البيانات القديمة والجديدة)
- ✅ معلومات المستخدم والوقت والـ IP
- ✅ فلترة متقدمة

### 4. معايير أمان عالية
- ✅ حماية SQL Injection
- ✅ التحقق من الصلاحيات على الخادم
- ✅ تسجيل جميع الأنشطة المريبة
- ✅ عزل البيانات حسب الصلاحيات

---

## 🚀 البدء السريع

### الخطوة 1️⃣: تحديث قاعدة البيانات

قم بتشغيل الملف `schema.sql`:

```bash
mysql -u root < schema.sql
```

أو من داخل phpMyAdmin:
- استورد ملف `schema.sql`

### الخطوة 2️⃣: إنشاء المستخدمين

زيارة الرابط في المتصفح:
```
http://localhost/accommodation/init_users.php
```

### الخطوة 3️⃣: تسجيل الدخول

```
الرابط: http://localhost/accommodation/login.php

البيانات التجريبية:
- username: admin
- password: admin123
```

### الخطوة 4️⃣: استكشف النظام

- 👥 اضغط على "المستخدمين" لإدارة المستخدمين
- 🛡️ اضغط على "الأدوار" لإدارة الأدوار والصلاحيات
- 📜 اضغط على "السجلات" لعرض الأنشطة

---

## 📱 صفحات النظام

### 1. صفحة تسجيل الدخول (login.php)
**الوصول**: `http://localhost/accommodation/login.php`

**الميزات**:
- تسجيل دخول آمن
- عرض بيانات تجريبية
- تحقق من الصلاحيات
- تسجيل محاولات الدخول

**المتطلبات**:
- جداول users و roles

---

### 2. الصفحة الرئيسية (index.php)
**الوصول**: `http://localhost/accommodation/index.php`

**المميزات الجديدة**:
- ✅ شريط معلومات المستخدم في الأعلى
- ✅ روابط سريعة للمستخدمين والأدوار والسجلات
- ✅ زر تسجيل خروج
- ✅ تحكم الأزرار حسب الصلاحيات

**المتطلبات**:
- تسجيل دخول
- صلاحية view_workers

---

### 3. إدارة المستخدمين (manage_users.php)
**الوصول**: من شريط المستخدم أو `manage_users.php`

**الميزات**:
- عرض جميع المستخدمين في جدول
- إضافة مستخدم جديد
- تعديل بيانات المستخدم
- حذف المستخدم
- تغيير حالة المستخدم (نشط/معطل)

**الصلاحيات**:
- `view_users` - عرض
- `add_user` - إضافة
- `edit_user` - تعديل
- `delete_user` - حذف

**الجدول**:
| # | اسم المستخدم | الاسم الكامل | البريد | الدور | الحالة | الإجراءات |
|---|---|---|---|---|---|---|
| 1 | admin | مدير النظام | admin@local | مدير النظام | نشط | تعديل، حذف |

---

### 4. إدارة الأدوار والصلاحيات (manage_roles.php)
**الوصول**: من شريط المستخدم أو `manage_roles.php`

**المميزات**:
- قائمة الأدوار على اليسار
- لوحة الصلاحيات على اليمين
- إضافة دور جديد
- تعديل وصف الدور
- حذف الدور
- إدارة الصلاحيات بشكل تفاعلي

**واجهة العمل**:
```
┌─────────────────────┬───────────────────────┐
│   قائمة الأدوار     │   الصلاحيات           │
├─────────────────────┼───────────────────────┤
│ • مدير النظام       │ ☑ عرض العاملات       │
│ • مشرف             │ ☑ إضافة عاملة        │
│ • موظف             │ ☑ تعديل              │
│ • مراجع            │ ☑ حذف                │
└─────────────────────┴───────────────────────┘
```

---

### 5. سجل الأنشطة (activity_logs.php)
**الوصول**: من شريط المستخدم أو `activity_logs.php`

**الميزات**:
- عرض جميع الأنشطة المسجلة
- فلترة حسب:
  - نوع الكيان
  - نوع الإجراء
  - المستخدم
  - التاريخ
- عرض التفاصيل والبيانات المتغيرة
- عرض معلومات المستخدم

**الجدول**:
| الإجراء | الوصف | المستخدم | النوع | التاريخ | الخيارات |
|---|---|---|---|---|---|
| 🟢 إضافة | إضافة عاملة | أحمد | العاملات | 2026-06-15 10:30 | عرض |

---

## 🔐 الصلاحيات

### 1. الأدوار الافتراضية

#### 👑 مدير النظام
- ✅ صلاحيات كاملة
- ✅ إدارة المستخدمين والأدوار
- ✅ عرض جميع السجلات

#### 🎯 مشرف
- ✅ إدارة العاملات والمكاتب
- ✅ عرض التقارير
- ✅ الأرشفة
- ❌ لا يمكنه إدارة المستخدمين

#### 👤 موظف
- ✅ عرض العاملات
- ✅ إضافة عاملات
- ✅ تصدير البيانات
- ✅ عرض التقارير
- ❌ لا يمكنه التعديل والحذف

#### 👁️ مراجع
- ✅ عرض فقط
- ❌ أي عمليات أخرى

### 2. قائمة الصلاحيات الكاملة

**صلاحيات العاملات** (workers):
- `view_workers` - عرض
- `add_worker` - إضافة
- `edit_worker` - تعديل
- `delete_worker` - حذف
- `archive_worker` - أرشفة
- `export_workers` - تصدير
- `import_workers` - استيراد
- `view_worker_logs` - عرض السجلات

**صلاحيات المكاتب** (offices):
- `view_offices` - عرض
- `add_office` - إضافة
- `edit_office` - تعديل
- `delete_office` - حذف
- `import_offices` - استيراد

**صلاحيات التقارير** (reports):
- `view_reports` - عرض عام
- `view_all_workers_report` - التقرير الشامل
- `view_admin_report` - التقرير الإداري

**صلاحيات المستخدمين** (users):
- `view_users` - عرض
- `add_user` - إضافة
- `edit_user` - تعديل
- `delete_user` - حذف
- `assign_roles` - تعيين الأدوار

**صلاحيات الأدوار** (roles):
- `view_roles` - عرض
- `add_role` - إضافة
- `edit_role` - تعديل
- `delete_role` - حذف
- `manage_permissions` - إدارة الصلاحيات

**صلاحيات السجلات** (logs):
- `view_activity_logs` - عرض

---

## 📊 سجل الأنشطة

### أنواع الإجراءات

| الرمز | الإجراء | الوصف | اللون |
|---|---|---|---|
| ➕ | ADD | إضافة سجل جديد | 🟢 أخضر |
| ✏️ | EDIT | تعديل سجل | 🟠 برتقالي |
| 🗑️ | DELETE | حذف سجل | 🔴 أحمر |
| 📦 | ARCHIVE | أرشفة سجل | 🟣 بنفسجي |
| 🔑 | LOGIN | تسجيل دخول | 🔵 أزرق |
| 🚪 | LOGOUT | تسجيل خروج | ⚫ أسود |
| 📥 | IMPORT | استيراد بيانات | 🔷 فيروزي |
| 📤 | EXPORT | تصدير بيانات | 🟢 أخضر فاتح |

### البيانات المُسجلة

لكل نشاط يتم تسجيل:
- **معرف المستخدم**: من قام بالعملية
- **نوع الإجراء**: ADD, EDIT, DELETE, إلخ
- **نوع الكيان**: workers, offices, users, إلخ
- **معرف الكيان**: معرف السجل المتأثر
- **البيانات القديمة**: البيانات قبل التعديل
- **البيانات الجديدة**: البيانات بعد التعديل
- **الوصف**: شرح مختصر للعملية
- **عنوان IP**: عنوان الحاسوب
- **التاريخ والوقت**: متى حدثت العملية

---

## 💻 أمثلة عملية

### مثال 1: إضافة مستخدم جديد

```php
// في manage_users.php
$stmt = $pdo->prepare('
    INSERT INTO users (username, email, full_name, password, role_id)
    VALUES (?, ?, ?, ?, ?)
');
$stmt->execute(['ali', 'ali@company.com', 'علي محمد', password_hash('pass123', PASSWORD_BCRYPT), 2]);

// سيتم تسجيل النشاط تلقائياً
logActivity($_SESSION['user_id'], 'ADD', 'users', $user_id, null, $new_data, "إضافة مستخدم جديد: علي محمد", $pdo);
```

### مثال 2: التحقق من الصلاحية

```php
// في أي صفحة
if (hasPermission('add_worker', $pdo)) {
    // أظهر زر الإضافة
    echo '<button>إضافة عاملة</button>';
} else {
    // أخفِ الزر
    echo '<p>ليس لديك صلاحية</p>';
}
```

### مثال 3: عرض سجل الأنشطة

```php
// الحصول على أنشطة عاملة معينة
$stmt = $pdo->prepare('
    SELECT * FROM activity_logs
    WHERE entity_type = "workers" AND entity_id = ?
    ORDER BY created_at DESC
');
$stmt->execute([123]);
$activities = $stmt->fetchAll();
```

---

## 🔧 كيفية إضافة ميزة جديدة مع تسجيل الأنشطة

### الخطوة 1: الإذن (Permission)

```sql
INSERT INTO permissions (permission_name, description, module)
VALUES ('new_permission', 'وصف الصلاحية', 'module_name');
```

### الخطوة 2: التحقق من الصلاحية

```php
if (!hasPermission('new_permission', $pdo)) {
    die('ليس لديك صلاحية');
}
```

### الخطوة 3: تسجيل النشاط

```php
// احصل على البيانات القديمة
$stmt = $pdo->prepare('SELECT * FROM table WHERE id = ?');
$stmt->execute([$id]);
$old_data = $stmt->fetch();

// قم بالعملية
// ...

// سجل النشاط
logActivity(
    $_SESSION['user_id'],
    'ACTION_TYPE',
    'entity_type',
    $id,
    $old_data,
    $new_data,
    'الوصف',
    $pdo
);
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: "ليس لديك صلاحية"

**الأسباب المحتملة**:
1. لم تسجل دخول
2. المستخدم ليس له الصلاحية
3. الدور لا يملك الصلاحية

**الحل**:
```php
// تحقق من تسجيل الدخول
if (!isset($_SESSION['user_id'])) {
    header('Location: login.php');
    exit;
}

// تحقق من الصلاحية
echo hasPermission('permission_name', $pdo) ? 'نعم' : 'لا';

// تحقق من الدور
$user = getUserInfo($_SESSION['user_id'], $pdo);
echo $user['role_name'];
```

### المشكلة: لا تظهر الأنشطة

**الحل**:
```php
// تأكد من أن جدول activity_logs موجود
SHOW TABLES LIKE 'activity_logs';

// تأكد من وجود بيانات
SELECT COUNT(*) FROM activity_logs;

// اختبر الدالة
logActivity($_SESSION['user_id'], 'TEST', 'test', null, null, null, 'اختبار', $pdo);
```

### المشكلة: خطأ SQL

**الحل**:
```php
try {
    // الكود
} catch (PDOException $e) {
    echo 'الخطأ: ' . $e->getMessage();
    echo 'في السطر: ' . $e->getLine();
}
```

---

## 📋 قائمة التحقق

- [ ] تشغيل schema.sql
- [ ] إنشاء المستخدمين
- [ ] تسجيل الدخول
- [ ] عرض صفحة المستخدمين
- [ ] إضافة مستخدم جديد
- [ ] عرض صفحة الأدوار
- [ ] تغيير الصلاحيات
- [ ] عرض سجل الأنشطة
- [ ] إضافة عاملة جديدة
- [ ] تحقق من السجل

---

## 📞 الدعم

للمزيد من المعلومات أو الإبلاغ عن مشاكل، راجع:
- `README_AUTH.md` - دليل شامل
- `ACTIVITY_LOG_GUIDE.php` - أمثلة عملية
- `INTEGRATION_GUIDE.php` - دليل الدمج

---

**آخر تحديث**: 2026-06-15
**الإصدار**: 2.0
**الحالة**: ✅ جاهز للاستخدام
